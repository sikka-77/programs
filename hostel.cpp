#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        char str[10][30]; //2D array for storing strings
        int i, n;
        cin >> n;

        for (i = 0; i < n; i++)
        {

            cin >> str[i];
        }

        int reading;
        cin >> reading;

        str[0] = "1";
    }

    return 0;
}